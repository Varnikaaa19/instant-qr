
# Instant QR Code Generator (Streamlit + segno)

A production-ready QR generator:
- Generate **PNG, SVG, PDF**
- Customize **error level, version, micro QR, colors, scale, quiet zone**
- Add **centered logo** safely
- **Download** individually or **batch** via CSV/TXT → ZIP
- Keeps **session history** with previews

## 🚀 Quick Start

```bash
# 1) Create and activate a virtual env (optional)
python -m venv .venv
# Windows
.venv\Scripts\activate
# macOS/Linux
source .venv/bin/activate

# 2) Install dependencies
pip install -r requirements.txt

# 3) Run the app
streamlit run app.py
``
